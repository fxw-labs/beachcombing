# 补点模板 — 新增赶海点条目

> 每个内置赶海点占一个条目。复制此模板，填入真实信息，追加到对应省份数据文件 `db/<province>.md`，然后运行 `scripts/validate_db.py` 校验。

---

## <点名称>（<省>·<地市>）

- **坐标**：<lat>, <lon>（一位减号+两位小数，如 `36.06, 120.38`）
- **滩涂类型**：`沙质 | 泥质 | 礁石 | 混合`
- **到达提示**：<如何导航/到达，一句>
- **潮汐基准点**：<最近的潮汐站名，便于 API 查询>

### 目标物种

- `<物种>`（`<科/别名>`）— <挖法/技巧>
- `<物种>`（`<科/别名>`）— <挖法/技巧>

### 安全与合规

- <该点特有的安全提示，如泥陷、暗流、保护区禁入>
- <是否有休渔/许可限制>

### 小记

- <时令、最佳季节、当地老饕提示、相关吃法——一到两句，可填可不填>

---

> 校验要求：坐标必须可解析；滩涂类型必须是枚举之一；物种条目的名称与别名不可为空；`<省>` 必须已在 `db/INDEX.md` 中登记。
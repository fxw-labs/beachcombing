---
name: beachcombing
description: 赶海潮汐与攻略查询工具。用于回答"去哪里赶海、什么时候去、退潮/低潮时间、能挖到什么、需要带什么、何时安全撤退"。触发词：赶海、退潮、潮汐、低潮、挖蛤蜊、捡海螺、抓螃蟹、大潮、小潮、海边挖货、beachcomb、tide、low tide、tidal flat。聚焦中国沿海。
origin: beachcombing
---

# 赶海 (Beachcombing) — 潮汐与赶海攻略

一个面向中国大陆的赶海查询 skill：解析"地点 + 时间 + 意向"，给出**可执行的赶海计划**——退潮黄金窗口、大/小潮、适用性评分、目标物种、出发清单、以及最重要的"安全撤退截止时刻"。

## 何时激活

- 用户提到"赶海 / 去海边挖货 / 退潮 / 低潮 / 潮汐"
- 用户给出地点（如青岛、舟山、厦门）和时间（如"这周六""明天下午"）
- 用户问"这个点能挖到什么 / 什么时候退潮 / 什么时候适合去"

## 核心原则（每次都必须遵守）

1. **安全撤退时刻置顶**：任何回复顶部第一要素必须是"下一个高潮时间 / 你必须在该时刻前离开滩涂"。绝不因语气活泼而稀释。大潮（潮差 > 2.5m）给**红色警告**。
2. **诚实原则**：永远给结果，但绝不假装精确。数据源全线不可用时，如实说明"无法提供精确潮时，请以当地潮汐 App 为准"，并附农历大潮骨架提示（初一/十五前后为大潮）。
3. **老饕语气**：报告有温度、有经验味，给技巧、时令、吃法；但安全信息保持严肃、无歧义。
4. **地点使用顺序**：内置热门点库（精确）→ geocoding 兜底 → 就近建议。查不到时优雅降级，不硬答。
5. **评分聚焦潮汐**：适用性评分只看潮汐（潮差 + 低潮时刻），天气在报告里展示但不计入评分。

## 报告结构（templates/report.md）

```
① 安全撤退截止时刻（最顶，真实数据/红色大潮警告）
② 地点概览（坐标、滩涂类型、到达提示）
③ 目标时段：低潮时间 + 黄金窗口（动态随潮差调整）
④ 适用性评分 ★ 分值 + 一句理由
⑤ 潮型与大潮指示
⑥ 目标物种（按地点预置/滩涂推断，含方言别名 + 吃法）
⑦ 天气（Open-Meteo，只看不评分；大风浪给警示）
⑧ 出发清单口诀
⑨ 合规与安全提示（休渔/保护区/许可，带免责）
⑩ 可选用下一步（换日期/换地点/附近点/导出日历）
```

## 工作流

1. **解析输入**：从自然语言提取 `地点、时间、意向`。地点可为精确点或城市。
2. **定位地点**：
   - 先在 `db/INDEX.md` 查内置热门点（最好，物种/安全/到达提示最准）。
   - 命中省级文件 → 读取对应数据。
   - 未命中 → 调用 `bin/beachcomb.py geocode <地名>` 解析坐标，或给最近内置点的至少两个邻近建议，让用户确认。
   - 内陆城市 → 提醒"该地无海岸"，推荐最近海岸点。
3. **拉取数据**：调用 `bin/beachcomb.py tide <lat> <lon> <date>`（多级降级：key API → 无 key 中国源 → 农历骨架），和 `bin/beachcomb.py weather <lat> <lon> <date>`。
4. **生成报告**：按报告结构输出 markdown。黄金窗口动态：`大潮 ±2 小时 / 中潮 ±1.5 小时 / 小潮 ±1 小时`。
5. **结束**：附下一步入口（换日期/换地点/附近点），不强求继续。

## 潮汐数据降级链路（Q11/Q25）

| 层级 | 来源 | 状态 |
|------|------|------|
| 1 | 用户提供的潮汐 API key（WorldTides/Storm Glass） | 可选增强，`BEACHCOMB_TIDE_API` |
| 2 | 无 key 中国潮汐源（内置适配） | v1 主链路，尽力而为 |
| 3 | 农历大潮骨架 + 诚实声明 | 兜底，醒目标注"⚠️ 估算，±1 小时"，评分强制降档 |

链路上层失败自动降级到下层。**估测值除非有 key 型真实数据，否则适用性评分不得超过 ★★**。

## 环境与依赖

- Python 3.10+
- 无第三方硬件依赖；网络请求走 stdlib `urllib`（离线时自动降级到农历骨架）。
- 可选：`BEACHCOMB_TIDE_API` 环境变量（key 型潮汐 API）。
- 天气：Open-Meteo 免费无 key（单点查询），失败不影响潮汐核心。

## Non-Goals（v1 明确不做，被问及时坦率说明）

- ❌ 钓鱼预报（钓潮是另一套指标体系）
- ❌ 实时冲浪 / 潜水 / 海洋海况等级
- ❌ 赶海生物影像识别（需 CV 能力）
- ❌ 精细法律核对（休渔期 / 保护区边界以当地官方为准，本 skill 只给提示、带免责）
- ❌ 本地谐波潮汐精确估算（避免误导，宁给农历骨架+诚实声明）

## 数据贡献（Q17）

欢迎扩充赶海点库。每个内置点以 `db/point-entry.md` 模板组织，新增后运行 `scripts/validate_db.py` 校验格式与索引一致性。

## 归属与免责

本 skill 提供辅助信息，是一种便利工具，不构成法律或安全建议。海况危险、潮汐变化、当地法规均以官方和现场实际为准。**出海赶海请务必：结伴、看官方潮汐表、涨潮前撤离、远离陌生滩涂。**